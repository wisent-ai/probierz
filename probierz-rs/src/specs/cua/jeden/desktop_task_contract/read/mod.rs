//! Reading back what the run recorded: the recorded session itself, and the
//! report the window has to show for it.

mod recorded;
mod report;
mod visible;

pub(crate) use recorded::*;
pub(crate) use report::*;
pub(crate) use visible::*;
